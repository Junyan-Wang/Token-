name: Token 价格监测

on:
  schedule:
    # UTC 时间，每 6 小时的第 23 分钟跑一次（约等于北京时间 08:23/14:23/20:23/02:23）
    # Token 价格变动低频，6 小时一查足够；GitHub 定时任务有 5~30 分钟随机延迟属正常
    - cron: "23 */6 * * *"
  workflow_dispatch: {}   # 允许在 Actions 页面手动点按钮触发

permissions:
  contents: write         # 允许把 state.json 写回仓库（去重用）

concurrency:
  group: monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4

      - name: 安装 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 运行监控
        env:
          SERVERCHAN_SENDKEY: ${{ secrets.SERVERCHAN_SENDKEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python monitor.py

      - name: 保存监控状态（价格快照）
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state.json
          git diff --cached --quiet || git commit -m "chore: update price snapshot [skip ci]"
          git push
